{% extends 'base.html.twig' %}

{% block title %}Panel{% endblock %}

{% block body %}
    <h1>Panel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panel.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panel_index') }}">back to list</a>

    <a href="{{ path('panel_edit', {'id': panel.id}) }}">edit</a>

    {{ include('panel/_delete_form.html.twig') }}
{% endblock %}
