<form method="post" action="{{ path('correspondre_delete', {'id': correspondre.id}) }}" onsubmit="return confirm('Vous êtes sur de vouloir supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ correspondre.id) }}">
    <button class="btn">-</button>
</form>
