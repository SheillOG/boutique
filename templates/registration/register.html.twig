{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <div class="text-center">
        <main class="bg-gray max-w-lg mx-auto p-8 md:p-12 my-10 rounded-lg shadow-2xl">
    <h1>Inscription</h1>

            <imput type = "text">Role: User</imput>
    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}


        <button type="submit" class="btn">S'inscrire</button>


    {{ form_end(registrationForm) }}
        </main>
    </div>







    {#<main class="bg-white max-w-lg mx-auto p-8 md:p-12 my-10 rounded-lg shadow-2xl">
        <section>
            <h3 class="font-bold text-2xl">Inscription</h3>
            <p class="text-gray-600 pt-2">Espace pour la creation de compte.</p>
        </section>

        <section class="mt-10">
            <form class="flex flex-col" method="POST" action="#">
                <div class="mb-6 pt-3 rounded bg-gray-200">
                    <label class="block text-gray-700 text-sm font-bold ml-3" for="email">Email</label>
                    #}{#                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>#}{#

                    {{ form_row(registrationForm.email) }}



#}{#                    <input type="email" value="" name="email" id="registrationForm.email" class="bg-gray-200 rounded w-full text-gray-700 focus:outline-none border-b-4 border-gray-300 focus:border-purple-600 transition duration-500 px-3 pb-3">#}{#
                </div>
                <div class="mb-6 pt-3 rounded bg-gray-200">
                    <label class="block text-gray-700 text-sm font-bold ml-3" for="password">Password</label>
                    #}{#                    <input type="password" name="password" id="inputPassword" class="form-control" required>#}{#


                    {{ form_row(registrationForm.plainPassword, {
                        label: 'Password'
                    }) }}

#}{#                    <input type="password" name="password" id="registrationForm.plainPassword" class="bg-gray-200 rounded w-full text-gray-700 focus:outline-none border-b-4 border-gray-300 focus:border-purple-600 transition duration-500 px-3 pb-3">#}{#
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    {{ form_row(registrationForm.agreeTerms) }}
                </div>

                <button class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-2 rounded shadow-lg hover:shadow-xl transition duration-200" type="submit">S'inscrire</button>
            </form>
        </section>
    </main>





    {{ form_end(registrationForm) }}
#}


{#    <footer class="max-w-lg mx-auto flex justify-center text-white">
        <a href="#" class="hover:underline">Contact</a>
        <span class="mx-3">•</span>
        <a href="#" class="hover:underline">Privacy</a>
    </footer>#}
    </body>
    </html>




{% endblock %}
